#include <sstream>
#include <iostream>
#include "Application.h"
int main()
{
    Application app;
    app.run();
    // double sideA, sideB, sideC;
    // sideA = 3.08;
    // sideB = 4.5;
    // sideC = 4.90;
    // std::ostringstream oss;
    // oss << "Triangle " << sideA << " " << sideB << " " << sideC;
    // std::cout << oss.str();
    // std::cout << "Triangle " + std::to_string(sideA) + " " + std::to_string(sideB) + " " + std::to_string(sideC);
}